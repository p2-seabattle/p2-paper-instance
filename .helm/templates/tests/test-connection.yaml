apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paper-instance.fullname" . }}-test-connection"
  labels:
    {{- include "paper-instance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paper-instance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
